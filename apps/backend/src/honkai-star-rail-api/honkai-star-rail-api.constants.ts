import { RelicPart, relicPart } from "@scepter/utilities";

export const relicPartMap: Record<string, RelicPart> = {
  HEAD: relicPart.head,
  HAND: relicPart.gloves,
  BODY: relicPart.body,
  FOOT: relicPart.boots,
  NECK: relicPart.sphere,
  OBJECT: relicPart.rope,
};
